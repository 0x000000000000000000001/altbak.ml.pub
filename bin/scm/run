#!/usr/bin/env bash
set -e
cd "$(dirname "$0")/../.."

mkdir -p bak/scm/output bak/scm/spago
rm -rf output .spago spago.yaml spago.lock
ln -s bak/scm/output output
ln -s bak/scm/spago .spago
ln -s bak/scm/spago.yaml spago.yaml
touch bak/scm/spago.lock && ln -s bak/scm/spago.lock spago.lock
npx spago build > /dev/null 2>&1
PATH="$(pwd)/bin/scm:$PATH" npx purescm build > /dev/null 2>&1
PATH="$(pwd)/bin/scm:$PATH" npx purescm bundle-app --main App > /dev/null 2>&1

echo "=== Chez Scheme ==="
export DYLD_LIBRARY_PATH="/opt/homebrew/lib:/opt/homebrew/opt/icu4c/lib"
time chez --program bak/scm/output/main
